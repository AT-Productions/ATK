#include <string>
#include "../../include/getVersion.h"

/**
 * Returns current version to console
 * when user asks it
*/
string getVersion(){
    const string version = "0.0.0.1";
    return version;
}
