#include "../../include/exitFailure.h"

#include <stdlib.h> // ? REMOVE IN FUTURE ?
/**
 * ? REMOVE IN FUTURE ?
 * returns EXIT_FAILURE
*/
int exitfailure(){
    exit(EXIT_FAILURE);

    // ? REMOVE IN FUTURE ?
    return EXIT_FAILURE;
}