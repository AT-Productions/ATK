#include <string>
#include "getVersion.h"

/**
 * Returns current version to console
 * when user asks it
*/
string getVersion(){
    const string version = "Current version for atk.exe: 0.0.1";
    return version + "\n";
}
